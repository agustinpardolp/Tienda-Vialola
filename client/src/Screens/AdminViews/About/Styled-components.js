import styled from "styled-components";
import { Form } from "semantic-ui-react";

const StyledForm = styled(Form)`
  width: 100%;
  padding: 3rem;
  .text-area{
    height: 25vh;
  }
`;

export { StyledForm };
